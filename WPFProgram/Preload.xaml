<Window x:Class="StarSaviorAssistant.Preload"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LoadingView" Height="350" Width="500"
        WindowStyle="None" ResizeMode="NoResize" 
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                            <Border x:Name="PART_Track"/>
                            <Border x:Name="PART_Indicator" 
                                    HorizontalAlignment="Left" 
                                    Background="{TemplateBinding Foreground}" 
                                    CornerRadius="5"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource CardBackground}" CornerRadius="15" BorderBrush="{DynamicResource MainBorder}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="스타세이비어 어시스턴트" Foreground="{DynamicResource MainForeground}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="프로그램 시작 준비중..." Foreground="{DynamicResource SubText}" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <DockPanel LastChildFill="False" Margin="0,0,0,5">
                    <TextBlock x:Name="TxtMainStatus" Text="Loading Core Components..." Foreground="{DynamicResource MainForeground}" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TxtMainPercent" Text="0%" Foreground="{DynamicResource PrimaryColor}" DockPanel.Dock="Right"/>
                </DockPanel>
                <ProgressBar x:Name="PbTotal" Value="0" Maximum="100" Height="12"/>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <DockPanel LastChildFill="False" Margin="0,0,0,5">
                    <TextBlock x:Name="TxtDetailStatus" Text="Checking files..." Foreground="{DynamicResource SubText}" FontSize="12" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtDetailPercent" Text="0%" Foreground="{DynamicResource PrimaryColor}" DockPanel.Dock="Right"/>
                </DockPanel>
                <ProgressBar x:Name="PbDetail" Value="0" Maximum="100" Height="6" Foreground="{DynamicResource SuccessGreen}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>